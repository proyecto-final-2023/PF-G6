import React from "react";

/**
 * todo: list :D
 * handles backend call to fetch memberships
 * handles search (may add later)
 */
export default function MembershipsContainer({ page }: { page: number }) {
  return <div>MembershipsContainer</div>;
}

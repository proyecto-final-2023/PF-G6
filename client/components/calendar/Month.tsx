// Libraries
// Types
// Components/Assets

import { WeekProps } from "./Week";

// ? * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * *
type MonthProps = {
  weeksData: WeekProps[];
};

export default function Month({ weeksData }) {
  return <div>Month</div>;
}
